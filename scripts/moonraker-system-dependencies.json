{
    "_comment": "The only place this repo declares apt packages. moonraker.conf names this path in system_dependencies:, and Moonraker's _verify_path only warns when it is wrong - so a moved file installs nothing and surfaces much later as an ImportError. tests/test_packaging.py checks the path instead.",
    "debian": [
        "python3-serial",
        "python3-pyudev"
    ]
}
